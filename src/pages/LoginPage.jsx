import { supabase } from "../supabaseClient";

function LoginPage() {
    return (
        <div className="auth-container">
            <p>login page</p>
        </div>
    )
}

export default LoginPage